#!/bin/sh
# $Id$
# Copyright (c) 2003,2004 Alexander Chernov

# This is a `compilation' script for Dr. Scheme language.
# The idea is to append "#!${MZSCHEMEPATH} -qr" string
# to the beginning of the file.
# Usage: mzscheme <input> <output>

MZSCHEMEPATH="@ac_cv_contest_mzscheme_path@"

if [ x"${MZSCHEMEPATH}" = x -o x"${MZSCHEMEPATH}" = xno ]
then
  echo "This language is not supported." >&2
  exit 1
fi

echo "#!${MZSCHEMEPATH} -qr" > $2
echo >> $2
cat $1 >> $2
chmod +x $2
exit 0
