#!/bin/sh
# $Id$
# Copyright (c) 2002-2005 Alexander Chernov <cher@unicorn.cmc.msu.ru>

GCJRUN="@ac_cv_contest_gcj@"
GCJDIR="@ac_cv_contest_gcj_dir@"

if [ x"${GCJRUN}" = x -o x"${GCJRUN}" = xno ]
then
  echo "This language is not supported." >&2
  exit 1
fi

if [ x"${GCJDIR}" != x ]
then
    PATH="${GCJDIR}:${PATH}"
fi

mv "$1" Main.java
exec "${GCJRUN}" ${EJUDGE_FLAGS} --main=Main -s -O2 Main.java -o "$2"
