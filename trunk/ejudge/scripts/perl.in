#!/bin/sh
# $Id$
# Copyright (c) 2003-2005 Alexander Chernov

# This is a `compilation' script for the Perl language.
# The idea is to append "#!${PERLPATH} -W" string
# to the beginning of the file.
# Usage: perl <input> <output>

# using EJUDGE_FLAGS we may pass additional flags

PERLPATH="@ac_cv_contest_perl_path@"

if [ x"${PERLPATH}" = x -o x"${PERLPATH}" = xno ]
then
  echo "This language is not supported." >&2
  exit 1
fi

echo "#!${PERLPATH} -W ${EJUDGE_FLAGS}" > $2
echo >> $2
cat $1 >> $2
chmod +x $2
exit 0
